{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "spark-chicken-api",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
